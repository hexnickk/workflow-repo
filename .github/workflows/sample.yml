name: Workflow Repo

on:
  push:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Target Workflow on main branch
        uses: hexnickk/action-repo@fad5bfb349b1ee93334e3be09473270b35d73e92
        
